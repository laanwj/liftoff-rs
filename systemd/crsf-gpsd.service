[Unit]
Description=Liftoff simulated GPS server
After=network.target zenohd.service

[Service]
Type=simple
ExecStart=%h/.cargo/bin/crsf-gpsd $LIFTOFF_GPSD_ARGS
Environment=RUST_LOG=info
EnvironmentFile=-%h/.config/liftoff/env
Restart=on-failure
RestartSec=3

[Install]
WantedBy=liftoff.target
