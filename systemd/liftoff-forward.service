[Unit]
Description=Liftoff serial CRSF bridge
After=network.target zenohd.service

[Service]
Type=simple
ExecStart=%h/.cargo/bin/liftoff-forward $LIFTOFF_FORWARD_ARGS
Environment=RUST_LOG=info
EnvironmentFile=-%h/.config/liftoff/env
Restart=on-failure
RestartSec=3

[Install]
WantedBy=liftoff.target
